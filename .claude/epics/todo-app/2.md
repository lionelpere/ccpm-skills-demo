# Task 001: Project Setup & Infrastructure

**status:** pending
**assignee:** unassigned
**estimated:** 2-3h
**epic:** todo-app
**depends_on:** none
**parallel_safe:** no

---

## Title
Initialize project structure and development environment

## Description
Create the foundational project structure with backend Spring Boot application, frontend React application, and local development infrastructure including PostgreSQL database.

## Acceptance Criteria

- [ ] Backend: Spring Boot project initialized with Maven
  - Use Spring Initializr (start.spring.io)
  - Group: com.todoapp
  - Artifact: todo-backend
  - Dependencies: Web, Data JPA, Security, PostgreSQL, Validation, Lombok

- [ ] Backend: Base package structure created
  - `config/`, `controller/`, `service/`, `repository/`, `model/`, `dto/`, `security/`, `exception/`

- [ ] Frontend: Vite + React + TypeScript project initialized
  - `npm create vite@latest todo-frontend -- --template react-ts`
  - Clean up default files

- [ ] H2 database configured (embedded, no Docker needed)
  - In-memory mode for demo
  - Console accessible at /h2-console
  - Auto-configured by Spring Boot

- [ ] Environment variables configured
  - Backend: `src/main/resources/application.properties`
  - Frontend: `.env` and `.env.example`

- [ ] Both projects run successfully
  - Backend: `mvn spring-boot:run` starts on port 8080
  - Frontend: `npm run dev` starts on port 5173
  - PostgreSQL accessible

- [ ] README with setup instructions
  - Prerequisites listed
  - Setup steps documented
  - Commands to run each component

## Technical Notes

### Backend Spring Boot Configuration

**application.properties:**
```properties
spring.application.name=todo-backend
spring.datasource.url=jdbc:postgresql://localhost:5432/todoapp
spring.datasource.username=todouser
spring.datasource.password=todopass
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

jwt.secret=${JWT_SECRET:your-256-bit-secret-change-in-production}
jwt.expiration=86400000

server.port=8080
```

### Docker Compose

**docker-compose.yml:**
```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: todo-postgres
    environment:
      POSTGRES_DB: todoapp
      POSTGRES_USER: todouser
      POSTGRES_PASSWORD: todopass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
```

### Frontend Environment

**.env.example:**
```env
VITE_API_BASE_URL=http://localhost:8080/api
```

## Files to Create

```
todo-app/
├── backend/
│   ├── src/main/java/com/todoapp/
│   │   ├── TodoAppApplication.java
│   │   ├── config/
│   │   ├── controller/
│   │   ├── service/
│   │   ├── repository/
│   │   ├── model/
│   │   ├── dto/
│   │   ├── security/
│   │   └── exception/
│   ├── src/main/resources/
│   │   └── application.properties
│   ├── pom.xml
│   └── README.md
├── frontend/
│   ├── src/
│   ├── .env.example
│   ├── .env
│   ├── package.json
│   ├── tsconfig.json
│   ├── vite.config.ts
│   └── README.md
├── docker-compose.yml
└── README.md
```

## Verification Steps

1. **Start PostgreSQL:**
   ```bash
   docker-compose up -d
   docker ps  # Should show postgres running
   ```

2. **Test Backend:**
   ```bash
   cd backend
   mvn spring-boot:run
   # Should start on http://localhost:8080
   # Check logs for successful database connection
   ```

3. **Test Frontend:**
   ```bash
   cd frontend
   npm install
   npm run dev
   # Should start on http://localhost:5173
   ```

## Definition of Done

- [ ] All acceptance criteria met
- [ ] Docker Compose starts PostgreSQL successfully
- [ ] Backend starts without errors and connects to database
- [ ] Frontend dev server starts and loads in browser
- [ ] README documents complete setup process
- [ ] `.gitignore` files configured for both projects
- [ ] Code committed to git

## Estimated Time Breakdown

- Spring Boot initialization: 30 min
- React/Vite setup: 20 min
- Docker Compose configuration: 20 min
- Directory structure creation: 15 min
- Environment configuration: 20 min
- README documentation: 30 min
- Testing and verification: 25 min

**Total:** 2-3 hours
